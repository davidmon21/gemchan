#!/usr/bin/env ruby
require_relative '../lib/gemchan/controller'
require_relative '../lib/gemchan/model'
require 'optparse'
require 'io/console'
require 'yaml/store'
@schema = 

def create(name)
    user= Gemchan::User.create(username: name)
    user.password= STDIN.noecho(&:gets).chomp
    user.save!
end

#options = { name: "admin", password: "password" }
options = {:name => nil}
opts = OptionParser.new do |opts|
    opts.banner = "Usage: #{$0} [options]"
  
    opts.on("-u", "--create-user user",
            "user name") do |name|
      options[:name] = name
    end
    opts.on("-i", "--install",
            "Install setup") do |name|
      options[:name] = name
    end
end.parse!

unless options[:name].nil?
  create(name)
end



#create(options[:name], options[:password])
#puts Gemchan::ChanController::board_page_data('/b').inspect
